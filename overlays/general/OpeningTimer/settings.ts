import type { OverlaySettings } from '@/lib/overlays/types'

const settings: OverlaySettings = {
  model: 'general-opening-timer',
  color: 4,
  isFullscreen: false,
  widgetName: 'Opening Timer',
}
export default settings
